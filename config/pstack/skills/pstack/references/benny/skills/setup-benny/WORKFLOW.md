# Set up Benny

Read the installed `setup-benny/SKILL.md` from the shared pstack skill root.
Use this copied pack's `FOR_AGENTS.md` as the user-intent contract. Follow the
portable setup workflow, preserving local configuration. The runtime must
already have pstack installed; if it does not, report that dependency before
activating either job.
